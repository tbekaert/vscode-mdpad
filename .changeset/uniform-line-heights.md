---
"mdpad": patch
---

Fix cursor jumping when scrolling through documents with mixed line heights. Headings now use only `font-weight` (no font-size change), and `mdpad.headingScale` setting is removed.
